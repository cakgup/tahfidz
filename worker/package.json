{
  "name": "hifz-companion-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 migrations apply hifz-companion-db --local",
    "d1:migrate:remote": "wrangler d1 migrations apply hifz-companion-db --remote"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}